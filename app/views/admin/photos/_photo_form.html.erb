<%= simple_form_for([:admin, @property, @photo], remote: true, html: { id: :submit_photo }) do |f| %>
  <%= f.input :key %>
  <%= f.input :picture %>
  <%= f.submit "Submit", class: "btn btn-success" %>
<% end %>
