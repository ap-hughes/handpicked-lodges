$(document).ready(function() {
  $("#modal-window").find(".modal-content").html("<%= j (render 'edit') %>");
  $("#modal-window").modal();
    $('#submit_feature').on('submit', function() {
    $('#modal-window').modal('hide');
  });
});
