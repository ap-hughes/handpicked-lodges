<%= simple_form_for ([:admin, @feature]) do |f| %>
  <%= f.association :property, label: "Feature for Lodge:" %>
  <%= f.input :name, label: "Feature" %>
  <%= f.input :key, label: "Key feature" %>
  <%= f.submit "Submit", class: "btn btn-success" %>
<% end %>
